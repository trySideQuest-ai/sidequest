## What

<!-- Brief: what does this change do? -->

## Why

<!-- Brief: motivation. Link to issue if any. -->

## Test plan

- [ ] `cd plugin && pytest -v` passes
- [ ] If touching skills: `/sidequest:sq-status` still reports healthy on a real install
- [ ] If touching the native app: tested locally on macOS 13+

## Privacy impact

<!-- Does this change what data is sent to the API? Does it touch ~/.sidequest/ files? Yes/no + one line. -->

## Releases

<!-- If this is plugin-shippable: check after a release tag is cut. -->
- [ ] Plugin tarball: `plugin-vX.Y.Z`
- [ ] App DMG: `app-vX.Y.Z`
